<h2><code>lf-textarea</code></h2>

<p>Component that displays a textarea (who would've thought, eh?):</p>

{{#code-preview triggers="preview,template,component" selectedTab="preview" as |selectedTab|}}
  {{#code-preview/outlet name="preview" selected=selectedTab}}
    {{examples/lf-textarea}}
  {{/code-preview/outlet}}

  {{#code-preview/outlet name="template" selected=selectedTab}}
    {{#code-sample lang="js"}}
\{{#lf-form rules=rules as |validateFunction|}}
  \{{lf-textarea
    label="Foo property"
    property=foo
    name="foo"
    rows=10
    validate=validateFunction
    on-update=(action (mut foo))
  }}
\{{/lf-form}}
    {{/code-sample}}
  {{/code-preview/outlet}}

  {{#code-preview/outlet name="component" selected=selectedTab}}
    {{#code-sample lang="js"}}
import Ember from 'ember';

const { Component } = Ember;

export default Component.extend({
  foo: '',
  rules: {
    foo: 'required'
  }
});
    {{/code-sample}}
  {{/code-preview/outlet}}

{{/code-preview}}

<h2>Attributes:</h2>
<ul>
  <li><code>rows</code>: amount of rows in the textarea</li>
</ul>

