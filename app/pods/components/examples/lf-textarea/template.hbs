{{#lf-form rules=rules as |validateFunction|}}
  {{lf-textarea
    label="Foo property"
    property=foo
    name="foo"
    rows=10
    validate=validateFunction
    on-update=(action (mut foo))
  }}
{{/lf-form}}
